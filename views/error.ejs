<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: sans-serif;
      background-color: #f8d7da;
      color: #721c24;
      margin: 0;
      padding: 40px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }
    .container {
      background-color: #f1b0b7;
      padding: 30px;
      border-radius: 10px;
      max-width: 600px;
      width: 100%;
      text-align: center;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    .error-stack {
      margin-top: 20px;
      background: #fff;
      color: #333;
      padding: 10px;
      text-align: left;
      font-size: 14px;
      border-radius: 5px;
      overflow-x: auto;
    }
    a {
      margin-top: 20px;
      display: inline-block;
      color: #004085;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Something went wrong!</h1>
    <p><%= message %></p>

    <% if (error && error.stack) { %>
      <div class="error-stack"><pre><%= error.stack %></pre></div>
    <% } %>

    <a href="/">Go back to Home</a>
  </div>
</body>
</html>
